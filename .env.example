# Standalone game app (Gosok Kartu). Hands off to the claim flow when done.
#
# Where the KLAIM button sends the user. Leave unset/empty to claim inline
# (the button just confirms on-screen instead of navigating away).
VITE_CLAIM_URL=https://wap.imobile.id/sehat1/register.php
